node_modules/
dist/
*.log
# Raw generation artifacts (intermediate files from sprite/image generation)
2026-*-*-*.png

# Scratch planning docs (AI-generated session artifacts)
docs/plans/

*.DS_Store

# Local-only handoff / Claude Code context
CLAUDE.md

# Local-only Windows launcher artifacts (user-specific paths)
peon-pet.ico
start-peon-pet.vbs
